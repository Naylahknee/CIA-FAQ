import { createFileRoute } from "@tanstack/react-router";
import { BellRing, ExternalLink, HeartPulse, MapPin, Search, ShieldCheck, Smartphone } from "lucide-react";
import { useMemo, useState } from "react";
import { revealSidebar } from "@/lib/sidebar";
import { AppShell } from "@/components/app-shell";
import { PageHeader } from "@/components/page-header";
import { facts } from "@/data/guide";
import { careDirectory } from "@/data/guide-sections";
import cvsLogo from "@/assets/providers/cvs.png.asset.json";
import emergencyOneLogo from "@/assets/providers/emergency-one.png.asset.json";
import northwellLogo from "@/assets/providers/northwell.png.asset.json";

const providerLogos: Record<string, { src: string; alt: string }> = {
  "CVS Pharmacy": { src: cvsLogo.url, alt: "CVS Pharmacy" },
  "Emergency One": { src: emergencyOneLogo.url, alt: "Emergency One Urgent Care" },
  "Vassar Brothers Medical Center": { src: northwellLogo.url, alt: "Northwell Health, Vassar Brothers Medical Center's health system" },
};

export const Route = createFileRoute("/safety")({ head: () => ({ meta: [{ title: "Safety & Support — CIA Hyde Park Family Guide" }, { name: "description", content: "Health, safety, emergency, and escalation guidance for CIA Hyde Park families." }, { property: "og:title", content: "Safety & Support — CIA Hyde Park Family Guide" }, { property: "og:description", content: "Health, safety, emergency, and escalation guidance for CIA Hyde Park families." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: SafetyPage });
function SafetyPage() {
  const [query, setQuery] = useState("");
  const everbridge = facts.find((fact) => fact.id === "everbridge");
  const groups = useMemo(() => careDirectory.map((group) => ({ ...group, items: group.items.filter(([name, address, phone]) => `${group.kind} ${name} ${address} ${phone}`.toLowerCase().includes(query.toLowerCase())) })).filter((group) => group.items.length), [query]);
  return <AppShell><main className="page-wrap page-main"><PageHeader eyebrow="Safety & Support" title="Sick, injured, or unsure where to start?" description={<>For immediate danger or a life-threatening emergency, call <a href="tel:911">911</a>.</>}><label className="page-search"><Search/><span className="sr-only">Search safety and support</span><input value={query} onChange={(event) => { setQuery(event.target.value); if (event.target.value.trim()) revealSidebar(); }} placeholder="Search pharmacies, urgent care, or hospitals…"/></label></PageHeader><div className="support-grid"><article><ShieldCheck/><span>1</span><h2>Prepare now</h2><p>Save insurance details, allergies, medications, an emergency contact, and the closest pharmacy.</p></article><article><HeartPulse/><span>2</span><h2>Choose the right care</h2><p>Use a pharmacy, urgent care, or emergency services according to the seriousness of the problem.</p></article><article><BellRing/><span>3</span><h2>Call before leaving</h2><p>Confirm current hours, services, insurance acceptance, and transportation.</p></article></div>{everbridge&&<section className="everbridge-panel"><Smartphone/><div><p className="eyebrow">Campus safety</p><h2>Everbridge 360</h2><p>{everbridge.parentA}</p><div className="app-downloads"><a className="app-download ios" href="https://apps.apple.com/us/app/everbridge-360/id1632864063" target="_blank" rel="noreferrer"><Smartphone/><span><small>For iPhone</small><strong>Everbridge 360</strong><em>Open in the Apple App Store</em></span></a><a className="app-download android" href="https://play.google.com/store/apps/details?id=com.everbridge.enterprise" target="_blank" rel="noreferrer"><Smartphone/><span><small>For Android</small><strong>Everbridge 360</strong><em>Open in Google Play</em></span></a></div><a className="everbridge-faq-link" href={everbridge.link} target="_blank" rel="noreferrer">Read the setup FAQ <ExternalLink/></a></div></section>}<section className="detail-panel"><h2>Local care directory</h2>{groups.length?<div className="care-grid">{groups.map((group)=><article key={group.kind}><h3>{group.kind}</h3>{group.items.map(([name,address,phone])=>{const logo=providerLogos[name];return <div className="care-row" key={name+address}>{logo&&<img className="provider-logo" src={logo.src} alt={logo.alt}/>}<strong>{name}</strong><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${name}, ${address}`)}`} target="_blank" rel="noreferrer"><MapPin/>{address}</a><a href={`tel:${phone.replace(/-/g,"")}`}>{phone}</a></div>;})}</article>)}</div>:<div className="empty-state"><Search/><h2>No care options found</h2><p>Try a provider type, name, or location.</p></div>}</section><a className="official-safety-link" href="https://catalog.ciachef.edu/" target="_blank" rel="noreferrer">Open the current CIA handbook <ExternalLink/></a><aside className="warning-panel"><BellRing/><div><strong>Community advice is not emergency guidance.</strong><p>Call each provider to verify current details. Use official campus contacts for health, safety, harassment, discrimination, or serious unresolved concerns.</p></div></aside></main></AppShell>;
}