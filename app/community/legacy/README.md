Superseded by the Family Village redesign in the parent directory. Kept for one
release so the previous behaviour can be diffed against the new views, then
deleted in a cleanup commit. Nothing imports these files.
